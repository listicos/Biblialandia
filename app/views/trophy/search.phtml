<?php use Phalcon\Tag; ?>

<?php echo $this->getContent(); ?>

<table width="100%">
    <tr>
        <td align="left">
            <?php echo $this->tag->linkTo(array("trophy/index", "Go Back")); ?>
        </td>
        <td align="right">
            <?php echo $this->tag->linkTo(array("trophy/new", "Create ")); ?>
        </td>
    </tr>
</table>

<table class="browse" align="center">
    <thead>
        <tr>
            <th>Id</th>
         </tr>
    </thead>
    <tbody>
    <?php foreach ($page->items as $trophy) { ?>
        <tr>
            <td><?php echo $trophy->id ?></td>
            <td><?php echo $this->tag->linkTo(array("trophy/edit/" . $trophy->id, "Edit")); ?></td>
            <td><?php echo $this->tag->linkTo(array("trophy/delete/" . $trophy->id, "Delete")); ?></td>
        </tr>
    <?php } ?>
    </tbody>
    <tbody>
        <tr>
            <td colspan="2" align="right">
                <table align="center">
                    <tr>
                        <td><?php echo $this->tag->linkTo("trophy/search", "First") ?></td>
                        <td><?php echo $this->tag->linkTo("trophy/search?page=" . $page->before, "Previous") ?></td>
                        <td><?php echo $this->tag->linkTo("trophy/search?page=" . $page->next, "Next") ?></td>
                        <td><?php echo $this->tag->linkTo("trophy/search?page=" . $page->last, "Last") ?></td>
                        <td><?php echo $page->current, "/", $page->total_pages ?></td>
                    </tr>
                </table>
            </td>
        </tr>
    </tbody>
</table>
