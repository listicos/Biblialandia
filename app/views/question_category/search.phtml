<?php use Phalcon\Tag; ?>

<?php echo $this->getContent(); ?>

<table width="100%">
    <tr>
        <td align="left">
            <?php echo $this->tag->linkTo(array("question_category/index", "Go Back")); ?>
        </td>
        <td align="right">
            <?php echo $this->tag->linkTo(array("question_category/new", "Create ")); ?>
        </td>
    </tr>
</table>

<table class="browse" align="center">
    <thead>
        <tr>
            <th>Id</th>
         </tr>
    </thead>
    <tbody>
    <?php foreach ($page->items as $question_category) { ?>
        <tr>
            <td><?php echo $question_category->id ?></td>
            <td><?php echo $this->tag->linkTo(array("question_category/edit/" . $question_category->id, "Edit")); ?></td>
            <td><?php echo $this->tag->linkTo(array("question_category/delete/" . $question_category->id, "Delete")); ?></td>
        </tr>
    <?php } ?>
    </tbody>
    <tbody>
        <tr>
            <td colspan="2" align="right">
                <table align="center">
                    <tr>
                        <td><?php echo $this->tag->linkTo("question_category/search", "First") ?></td>
                        <td><?php echo $this->tag->linkTo("question_category/search?page=" . $page->before, "Previous") ?></td>
                        <td><?php echo $this->tag->linkTo("question_category/search?page=" . $page->next, "Next") ?></td>
                        <td><?php echo $this->tag->linkTo("question_category/search?page=" . $page->last, "Last") ?></td>
                        <td><?php echo $page->current, "/", $page->total_pages ?></td>
                    </tr>
                </table>
            </td>
        </tr>
    </tbody>
</table>
